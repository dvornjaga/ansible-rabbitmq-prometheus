global:
  scrape_interval: 15s
  evaluation_interval: 15s
  query_log_file: /log/query.log

alerting:
  alertmanagers:
  - static_configs:
    - targets:

rule_files:

scrape_configs:
  - job_name: 'rabbitmq'
    static_configs:
    - targets: ['{{ hostvars[groups["rabbitmq"][0]]["ansible_host"] }}:15692']